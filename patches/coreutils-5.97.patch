diff -ur /nfs/stock/coreutils-5.97/lib/atexit.c /u/root/src/coreutils-5.97/lib/atexit.c
--- /nfs/stock/coreutils-5.97/lib/atexit.c	Thu Sep 22 06:05:39 2005
+++ /u/root/src/coreutils-5.97/lib/atexit.c	Mon Feb 13 04:36:55 2023
@@ -10,6 +10,14 @@
 {
   /* If the system doesn't provide a definition for atexit, use on_exit
      if the system provides that.  */
+
+#if defined(_AIX) && defined(__i386__)
+ /* Nothing for now */
+#else
+
   on_exit (f, 0);
+
+#endif
+
   return 0;
 }
diff -ur /nfs/stock/coreutils-5.97/lib/fsusage.c /u/root/src/coreutils-5.97/lib/fsusage.c
--- /nfs/stock/coreutils-5.97/lib/fsusage.c	Thu Sep 22 06:05:39 2005
+++ /u/root/src/coreutils-5.97/lib/fsusage.c	Mon Feb 13 07:01:08 2023
@@ -38,6 +38,14 @@
 
 #include <limits.h>
 
+#if defined(_AIX) && defined(__i386__)
+#include <errno.h>
+/* #include <sys/fstypes.h> */
+#include <sys/ino.h>
+#define Fs2b    2               /* 1024-byte blocksize                     */
+#define INOPB(s) ((s) == 2? 1024/sizeof(struct dinode) : -1) 
+#endif
+
 #if HAVE_SYS_PARAM_H
 # include <sys/param.h>
 #endif
@@ -141,6 +149,12 @@
 #  define SUPERBOFF (SUPERB * 512)
 # endif
 
+#if defined(_AIX) && defined(__i386__)
+# define FILSYS_S_TYPE(fsd) (Fs2b)
+#else
+# define FILSYS_S_TYPE(fsd) (fsd.s_type)
+#endif
+
   struct filsys fsd;
   int fd;
 
@@ -161,14 +175,14 @@
     }
   close (fd);
 
-  fsp->fsu_blocksize = (fsd.s_type == Fs2b ? 1024 : 512);
+  fsp->fsu_blocksize = (FILSYS_S_TYPE(fsd) == Fs2b ? 1024 : 512);
   fsp->fsu_blocks = PROPAGATE_ALL_ONES (fsd.s_fsize);
   fsp->fsu_bfree = PROPAGATE_ALL_ONES (fsd.s_tfree);
   fsp->fsu_bavail = PROPAGATE_TOP_BIT (fsd.s_tfree);
   fsp->fsu_bavail_top_bit_set = EXTRACT_TOP_BIT (fsd.s_tfree) != 0;
   fsp->fsu_files = (fsd.s_isize == -1
 		    ? UINTMAX_MAX
-		    : (fsd.s_isize - 2) * INOPB * (fsd.s_type == Fs2b ? 2 : 1));
+		    : (fsd.s_isize - 2) * INOPB(FILSYS_S_TYPE(fsd)) * (FILSYS_S_TYPE(fsd) == Fs2b ? 2 : 1));
   fsp->fsu_ffree = PROPAGATE_ALL_ONES (fsd.s_tinode);
 
 #endif /* STAT_READ_FILSYS */
diff -ur /nfs/stock/coreutils-5.97/lib/nanosleep.c /u/root/src/coreutils-5.97/lib/nanosleep.c
--- /nfs/stock/coreutils-5.97/lib/nanosleep.c	Thu Sep 22 06:05:39 2005
+++ /u/root/src/coreutils-5.97/lib/nanosleep.c	Mon Feb 13 04:28:05 2023
@@ -36,6 +36,10 @@
 
 #include "timespec.h"
 
+#if defined(_AIX) && defined(__i386__)
+typedef int sig_atomic_t;
+#endif
+
 /* Some systems (MSDOS) don't have SIGCONT.
    Using SIGTERM here turns the signal-handling code below
    into a no-op on such systems. */
diff -ur /nfs/stock/coreutils-5.97/man/ln.1 /u/root/src/coreutils-5.97/man/ln.1
--- /nfs/stock/coreutils-5.97/man/ln.1	Thu May 25 17:27:35 2006
+++ /u/root/src/coreutils-5.97/man/ln.1	Mon Feb 13 07:03:24 2023
@@ -1,5 +1,5 @@
 .\" DO NOT MODIFY THIS FILE!  It was generated by help2man 1.35.
-.TH LN "1" "May 2006" "ln 5.97" "User Commands"
+.TH LN "1" "February 2023" "ln 5.97" "User Commands"
 .SH NAME
 ln \- make links between files
 .SH SYNOPSIS
diff -ur /nfs/stock/coreutils-5.97/src/system.h /u/root/src/coreutils-5.97/src/system.h
--- /nfs/stock/coreutils-5.97/src/system.h	Wed Sep 28 22:11:06 2005
+++ /u/root/src/coreutils-5.97/src/system.h	Mon Feb 13 07:08:07 2023
@@ -798,3 +798,17 @@
      || (Type) ((Accum) * 10 + (Digit_val)) < (Accum))			\
     ? false : (((Accum) = (Accum) * 10 + (Digit_val)), true))		\
   )
+
+#if defined(_AIX) && defined(__i386__)
+
+typedef int sig_atomic_t;
+
+#include <stat.h>
+
+int statx(const char * path, struct stat * buf, int len, int cmd);
+int symlink(const char * path1, const char * path2);
+int link(const char * path1, const char * path2);
+int lstat (const char * path, struct stat * buf);
+
+#endif
+
